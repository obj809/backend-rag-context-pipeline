# Pinned to a known-good set. sentence-transformers/numpy match the indexer +
# engine repos so embeddings line up. torch is installed from the CPU-only index
# in the Dockerfile (pinned there).
fastapi==0.136.3
uvicorn[standard]==0.49.0
langchain-core==1.4.6
langchain-openai==1.3.0
sentence-transformers==5.5.1
numpy==2.4.6
python-dotenv==1.2.2
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
pgvector==0.4.2

# Query engine, installed as a versioned package. Bump this tag to adopt engine changes. 
# Pulls langchain-core/-openai, sentence-transformers, numpy, psycopg, pgvector
# transitively; they stay listed above as the backend's own direct imports.
# For active engine dev, swap locally for: -e ../engine-rag-context-pipeline
rag-engine @ git+https://github.com/obj809/engine-rag-context-pipeline.git@v0.1.0

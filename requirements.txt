fastapi
uvicorn[standard]
langchain-core
langchain-openai
sentence-transformers
numpy
python-dotenv
psycopg[binary]
psycopg-pool
pgvector

# Query engine, installed as a versioned package (no sys.path bridge, no
# side-by-side checkout). Bump this tag to adopt engine changes. Pulls
# langchain-core/-openai, sentence-transformers, numpy, psycopg, pgvector
# transitively; they stay listed above as the backend's own direct imports.
# For active engine dev, swap locally for: -e ../engine-rag-context-pipeline
rag-engine @ git+https://github.com/obj809/engine-rag-context-pipeline.git@v0.1.0
